function createHobbit(name, age = 0){
  const dragon = {name: input_name, rider: input_rider, temperment: input_temperment, timesEaten: 0, hungry: true}
 
}

module.exports = {
  createHobbit, 
  // celebrateBirthday, 
  // getRing, 
  // meetPeople, 
  // findFriends
}